#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Random number generator
 * tell if a number is positive, negative or zero
 *
 * Return: 0
*/
int main(void)
{
	int n;

	n = 0;
	while (n < 10) {
		printf("%d", n);
		n++;
	}
	printf("%s", "\n");
	return (0);
}
